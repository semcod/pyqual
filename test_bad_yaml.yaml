# Test YAML file with intentional syntax errors
pipeline:
  name: test-pipeline
  stages:
  - name: test
    tool: pytest
    - name: lint
      run: "ruff check .  # unclosed quote"
  metrics:
    coverage_min: 80